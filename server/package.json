{
  "name": "kirgu-mini-app-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Backend для Telegram Mini App КИРГУ: REST API поверх адаптера каталога (Bitrix)",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "fastify": "^4.28.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.6.2"
  }
}
